# Demos

Run the demos locally:

```bash
# Vanilla JS
cd lib/demo/vanilla && npm install && npm run dev

# React
cd lib/demo/react && npm install && npm run dev
```

Build for production:

```bash
cd lib/demo/vanilla && npm run build
cd lib/demo/react && npm run build
```
